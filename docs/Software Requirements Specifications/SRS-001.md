# TODO

This extension should be an easy runnable, that is, an extension that you can setup custom commands to the vscode command pallete to the project itself to act as a workspace or user command pallete.

## Requirement 01

Create a software requirements specification to build a vscode plugin to, under the tools/vscode/command-pallete you can instantiate commands that can be cathegorized like:

<Cathegory>: <Name>

So the workspace folder also can host user commands to be available in the vscode command pallete.SQLite3

Also, the extension should use iNotify to monitor directory and extension changes to validate if the new command is valid or not.

## Requirement 02

Each command is a single python lua or js file with it's json descriptor specifying name category and source paths.

## Requirement 03

The extension should look for directory modifications in the user and workspace context and autoload the command pallet on directory change

in the workspace iºt should be in the tools/vscode/command-pallete

and the user under <vscode user extension data directory>/<extension-name>/command-pallete/command

## Requirement 04

The tree should be like:

meta.json
src/main.[js,lua,py]

than the meta.json

with at least

```json
{
    "category": "Category",
    "name": "Command",
    "sources": ["src/main.js"]
}
```


What do you think?
